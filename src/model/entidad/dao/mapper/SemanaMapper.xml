<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC '-//mybatis.org//DTD Mapper 3.0//EN'
  'http://mybatis.org/dtd/mybatis-3-mapper.dtd'>
  <mapper namespace='model.entidad.dao.mapper.SemanaMapper'>
  
  <select resultType="Semana" id="SemanaPorId">
  		SELECT * FROM SEMANA
		WHERE ID = #{id}
  </select>
  
  </mapper>